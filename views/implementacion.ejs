<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Implementación - Sistema de Auditorías ISO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .clausula-card {
            margin-bottom: 20px;
            border: 1px solid #dee2e6;
            border-radius: 5px;
        }
        .clausula-header {
            background-color: #f8f9fa;
            padding: 15px;
            border-bottom: 1px solid #dee2e6;
            cursor: pointer;
        }
        .clausula-body {
            padding: 15px;
        }
        .archivo-info {
            background-color: #e9ecef;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 10px;
        }
        .btn-group-sm {
            margin-top: 10px;
        }
        .upload-form {
            margin-top: 15px;
            padding: 15px;
            background-color: #f8f9fa;
            border-radius: 5px;
            border: 1px dashed #dee2e6;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <h1 class="mb-4">Implementación ISO 9001:2015</h1>
                <p class="lead">Descarga las plantillas, complétalas y súbelas para tu proceso de implementación.</p>

                <% if (messages.success_msg) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <%= messages.success_msg %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>

                <% if (messages.error_msg) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= messages.error_msg %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>

                <div class="accordion" id="plantillasAccordion">
                    <% plantillas.forEach(plantilla => { %>
                        <div class="card clausula-card">
                            <div class="card-header clausula-header" id="heading<%= plantilla.id %>" data-bs-toggle="collapse" data-bs-target="#collapse<%= plantilla.id %>" aria-expanded="false" aria-controls="collapse<%= plantilla.id %>">
                                <h5 class="mb-0">
                                    <i class="fas fa-chevron-down me-2"></i>
                                    Cláusula <%= plantilla.clausula %>: <%= plantilla.nombre %>
                                </h5>
                            </div>

                            <div id="collapse<%= plantilla.id %>" class="collapse" aria-labelledby="heading<%= plantilla.id %>" data-bs-parent="#plantillasAccordion">
                                <div class="card-body">
                                    <p><%= plantilla.descripcion %></p>

                                    <% if (archivosUsuario[plantilla.id]) { %>
                                        <div class="archivo-info">
                                            <h6><i class="fas fa-file-excel text-success me-2"></i>Archivo subido:</h6>
                                            <p class="mb-1">Nombre: <%= path.basename(archivosUsuario[plantilla.id].archivo_path) %></p>
                                            <p class="mb-1">Fecha: <%= new Date(archivosUsuario[plantilla.id].fecha_subida).toLocaleDateString() %></p>
                                            <form action="/implementacion/eliminar/<%= archivosUsuario[plantilla.id].id %>" method="POST" class="d-inline">
                                                <button type="submit" class="btn btn-sm btn-outline-danger mt-2" onclick="return confirm('¿Estás seguro de que quieres eliminar este archivo?')">
                                                    <i class="fas fa-trash me-1"></i>Eliminar
                                                </button>
                                            </form>
                                        </div>
                                    <% } %>

                                    <div class="btn-group-sm">
                                        <a href="/implementacion/descargar/<%= plantilla.id %>" class="btn btn-primary">
                                            <i class="fas fa-download me-1"></i>Descargar Plantilla
                                        </a>
                                        
                                        <button class="btn btn-outline-primary" type="button" data-bs-toggle="collapse" data-bs-target="#uploadForm<%= plantilla.id %>" aria-expanded="false" aria-controls="uploadForm<%= plantilla.id %>">
                                            <i class="fas fa-upload me-1"></i>Subir Plantilla Completada
                                        </button>
                                    </div>

                                    <div class="collapse mt-3" id="uploadForm<%= plantilla.id %>">
                                        <div class="upload-form">
                                            <form action="/implementacion/subir/<%= plantilla.id %>" method="POST" enctype="multipart/form-data">
                                                <div class="mb-3">
                                                    <label for="archivo<%= plantilla.id %>" class="form-label">Seleccionar archivo Excel (.xlsx, .xls)</label>
                                                    <input class="form-control" type="file" id="archivo<%= plantilla.id %>" name="archivo" accept=".xlsx,.xls" required>
                                                </div>
                                                <button type="submit" class="btn btn-success">
                                                    <i class="fas fa-cloud-upload-alt me-1"></i>Subir Archivo
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mantener abierto el acordeón después de subir un archivo
        document.addEventListener('DOMContentLoaded', function() {
            const urlParams = new URLSearchParams(window.location.search);
            const expandedItem = urlParams.get('expanded');
            if (expandedItem) {
                const element = document.getElementById('collapse' + expandedItem);
                if (element) {
                    new bootstrap.Collapse(element, { toggle: true });
                }
            }
        });
    </script>
</body>
</html>